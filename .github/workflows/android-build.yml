name: Build APK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      ANDROID_NDK: /home/runner/android-ndk-r25b
      ANDROID_NDK_HOME: /home/runner/android-ndk-r25b
      ANDROID_NDK_ROOT: /home/runner/android-ndk-r25b
      ANDROIDNDK: /home/runner/android-ndk-r25b
      ANDROID_SDK_ROOT: /home/runner/android-sdk
      ANDROID_HOME: /home/runner/android-sdk
      ANDROIDAPI: "30"
      ANDROIDMINAPI: "21"
      PATH: /home/runner/android-sdk/cmdline-tools/latest/bin:/home/runner/android-sdk/platform-tools:/usr/bin:/bin:/usr/lib/jvm/java-17-openjdk-amd64/bin:/home/runner/.local/bin:$PATH

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install buildozer cython

      - name: Remove incorrect NDK versions
        run: |
          rm -rf ~/.buildozer/android/platform/android-ndk-*
          rm -rf /usr/local/lib/android/sdk/ndk/*
          echo "Old NDKs removed."

      - name: Prepare directories and download NDK
        run: |
          NDK_DIR="/home/runner/.buildozer/android/platform"
          NDK_ZIP="$NDK_DIR/android-ndk-r25b-linux.zip"
          mkdir -p $NDK_DIR
          if [ ! -f $NDK_ZIP ]; then
            echo "NDK zip not found. Downloading..."
            wget https://dl.google.com/android/repository/android-ndk-r25b-linux.zip -O $NDK_ZIP
          else
            echo "NDK zip already exists, skipping download."
          fi
          if [ -f $NDK_ZIP ]; then
            echo "NDK archive found, extracting..."
            unzip -q $NDK_ZIP -d $NDK_DIR
          else
            echo "NDK archive not found. Exiting..."
            exit 1
          fi
          mv $NDK_DIR/android-ndk-r25b $NDK_DIR/../android-ndk-r25b

      - name: Install Android SDK tools and build-tools
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip zip tar openjdk-17-jdk wget
          mkdir -p $ANDROID_HOME/cmdline-tools
          cd $ANDROID_HOME/cmdline-tools
          wget https://dl.google.com/android/repository/commandlinetools-linux-10406996_latest.zip -O tools.zip
          unzip tools.zip -d temp
          rm tools.zip
          mkdir -p latest
          mv temp/cmdline-tools/* latest/
          rm -rf temp
          export PATH=$ANDROID_HOME/cmdline-tools/latest/bin:$PATH
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses || true
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager \
            "platform-tools" "platforms;android-30" "build-tools;30.0.3" "build-tools;36.0.0"
          mkdir -p /home/runner/.buildozer/android/platform/android-sdk/tools/bin
          ln -sfn /home/runner/android-sdk/cmdline-tools/latest/bin/sdkmanager /home/runner/.buildozer/android/platform/android-sdk/tools/bin/sdkmanager
          mkdir -p ~/.buildozer/android/platform/
          ln -sfn /home/runner/android-sdk ~/.buildozer/android/platform/android-sdk
          mkdir -p /home/runner/.buildozer/android/platform/android-sdk/build-tools
          echo "Installing build-tools 30.0.3"
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "build-tools;30.0.3"
          echo "Listing build-tools contents"
          ls /home/runner/android-sdk/build-tools/30.0.3/
          echo "Checking if AIDL exists in build-tools directory"
          if [ -f /home/runner/android-sdk/build-tools/30.0.3/aidl ]; then
            echo "AIDL found!"
          else
            echo "AIDL missing. Attempting to reinstall build-tools."
            $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "build-tools;30.0.3"
          fi

      - name: Set Buildozer NDK path
        run: |
          if [ ! -f buildozer.spec ]; then
            buildozer init
          fi
          sed -i '/^android.ndk_path/d' buildozer.spec
          echo "android.ndk_path = /home/runner/android-ndk-r25b" >> buildozer.spec

      - name: Fix AIDL location for Buildozer
        run: |
          echo "Fixing AIDL location for Buildozer..."
          AIDL_SRC="/home/runner/android-sdk/build-tools/30.0.3/aidl"
          AIDL_DEST_DIR="/home/runner/.buildozer/android/platform/android-sdk/build-tools/30.0.3"
          AIDL_DEST="$AIDL_DEST_DIR/aidl"
          mkdir -p "$AIDL_DEST_DIR"
          if [ -f "$AIDL_SRC" ]; then
            echo "AIDL found at $AIDL_SRC, copying to $AIDL_DEST"
            cp "$AIDL_SRC" "$AIDL_DEST"
            chmod +x "$AIDL_DEST"
          else
            echo "AIDL not found at $AIDL_SRC. Failing build."
            exit 1
          fi

      - name: Install 32-bit libraries for AIDL
        run: |
          sudo dpkg --add-architecture i386
          sudo apt-get update
          sudo apt-get install -y libc6:i386 libncurses6:i386 libstdc++6:i386 zlib1g:i386
          sudo apt-get install -y lib32z1 lib32ncurses6 lib32stdc++6

      - name: Reinstall 32-bit libraries for AIDL
        run: |
          sudo dpkg --add-architecture i386
          sudo apt-get update
          sudo apt-get install -y libc6:i386 libncurses6:i386 libstdc++6:i386 zlib1g:i386
          sudo apt-get install -y lib32z1 lib32ncurses6 lib32stdc++6

      - name: Verify AIDL binary architecture
        run: |
          file /home/runner/.buildozer/android/platform/android-sdk/build-tools/30.0.3/aidl
          ldd /home/runner/.buildozer/android/platform/android-sdk/build-tools/30.0.3/aidl || true

      - name: Ensure AIDL is executable
        run: |
          chmod +x /home/runner/.buildozer/android/platform/android-sdk/build-tools/30.0.3/aidl
          ls -l /home/runner/.buildozer/android/platform/android-sdk/build-tools/30.0.3/aidl

      - name: Replace AIDL with a compatible version from Google
        run: |
          echo "Downloading and extracting AIDL from build-tools archive..."
          AIDL_DEST_DIR="/home/runner/.buildozer/android/platform/android-sdk/build-tools/30.0.3"
          mkdir -p "$AIDL_DEST_DIR"
          wget https://dl.google.com/android/repository/build-tools_r30.0.3-linux.zip -O build-tools.zip
          unzip -o -j build-tools.zip 'android-*/aidl' -d "$AIDL_DEST_DIR"
          chmod +x "$AIDL_DEST_DIR/aidl"
          rm build-tools.zip

      - name: Fallback: Replace with known working AIDL (build-tools 29.0.2)
        run: |
          echo "Attempting fallback AIDL from build-tools 29.0.2"
          AIDL_DEST_DIR="/home/runner/.buildozer/android/platform/android-sdk/build-tools/30.0.3"
          mkdir -p "$AIDL_DEST_DIR"
          wget https://dl.google.com/android/repository/build-tools_r29.0.2-linux.zip -O build-tools-29.zip
          unzip -o -j build-tools-29.zip 'android-*/aidl' -d "$AIDL_DEST_DIR"
          chmod +x "$AIDL_DEST_DIR/aidl"
          rm build-tools-29.zip

      - name: Final verify AIDL binary
        run: |
          echo "Final check for aidl binary:"
          file /home/runner/.buildozer/android/platform/android-sdk/build-tools/30.0.3/aidl || true
          ldd /home/runner/.buildozer/android/platform/android-sdk/build-tools/30.0.3/aidl || true
          chmod +x /home/runner/.buildozer/android/platform/android-sdk/build-tools/30.0.3/aidl
          ls -la /home/runner/.buildozer/android/platform/android-sdk/build-tools/30.0.3/aidl

      - name: Build APK
        run: |
          buildozer -v android debug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4.6
        with:
          name: kivy-apk
          path: bin/*.apk
